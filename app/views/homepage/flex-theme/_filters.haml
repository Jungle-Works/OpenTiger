- if show_price_filter || show_custom_fields || show_date_filter
  - if show_price_filter
    = render :partial => "homepage/flex-theme/price_filter"

  - if show_date_filter
    = render :partial => "homepage/flex-theme/date_filter"

  - if show_custom_fields
    = render :partial => "homepage/flex-theme/custom_filters", locals: { filters: filters }

  -# %button{type: 'submit'}
  -#   = t("homepage.custom_filters.update_view")
